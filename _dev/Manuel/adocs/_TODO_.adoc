== BUGS ==

- Quand on annule un service commun, les données s'enregsitrent quand même…

== Todo list ==

- Outil pour définir des propriétés accessoires du projet
  - couleur de fond,
  - icone à utiliser (svg, png, etc.)

- Autres services : 
  - git init (tout le flux, juste en donnant le nom du repo sur Github)
  - installation documentation (regrouper tout ce qui concerne la documentation)

- meta + click = modification du service personnalisé (fait avec les commons)
- Maj+click sur service pour le sélectionner => boutons services ("↑", "↓", delete)

== Idées de nouveaux services (2026-07-11) ==

- Lancer un serveur de dev (`npm run dev`, `rails server`...) — terminal dédié, pas juste "jouer un script"
- Ouvrir le repo Git (statut, ou `git pull`/`push`)
- Ouvrir l'URL locale du projet dans le navigateur

- pouvoir définir 'données de l'app' : 
 - le navigateur par défaut
 - l'éditeur de documentation (asciidoctor) par défaut  (CotEditor)
 - le traitement de texte par défaut
== BUGS ==

- BUG #4 Gérer le cas où on utilise gh sans git init dans le dossier
- BUG #3 Quand un service remonte ok:true mais une erreur, cette erreur doit être affichée.
- BUG #2 Pour les rappels programmés, supprimer toutes les secondes
- BUG #1 les panneaux doivent tous s'ouvrir là où on a placé le dernier (-> TEST)


- Les rappels/alertes programmés ne fonctionnent pas
  Pourtant : 
    - ils sont bien registrés
    - Reminder est bien runing

- les erreurs mineures (footer) quand un panneau est ouvert, ne se lisent pas
  essayer en essayant de modifier une tâche sans en avoir sélectionné une

- Problème d'icône dans la documentation : fonctionne hors app, pas avec l'app

== Todo En cours

- Improve
  - update docu : partir du principe que le fichier s'appelle 'docu.adoc' ou 'manuel.adoc'
  - open docu : partir du principe que le fichier s'appelle du nom du fichier .adoc avec .html
  - open folder docu (nouveau) : si le fichier update ou open est existent : c'est ce dossier

== Todo list ==

- surveiller des mots ? (:error quand on est en train d'écrire (ou pas : la page pourrait surveille ":" ou ":[a-z]" et passer en mode pour insérer quelque chose)). :note, :idee, :error, 
- pouvoir mettre une note gh (github) du type voulu
  
== Idées d'autres services ==

- git init (tout le flux, juste en donnant le nom du repo sur Github)
- Lancer un serveur de dev (`npm run dev`, `rails server`...) — terminal dédié, pas juste "jouer un script"
- Ouvrir le repo Git (statut, ou `git pull`/`push`)
- Ouvrir l'URL locale du projet dans le navigateur

== Autres versions ==

- pour le type 'select', il faudrait un select customisé (le même que dans Eventer) avec possibilité de filtrer les valeurs.

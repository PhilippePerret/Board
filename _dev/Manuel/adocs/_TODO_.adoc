== BUGS ==

- Il faut pouvoir dire à un rappel que c'est bon, on a démarré la tâche

- les erreurs mineures (footer) quand un panneau est ouvert, ne se lisent pas
  essayer en essayant de modifier une tâche sans en avoir sélectionné une

- Problème d'icône dans la documentation : fonctionne hors app, pas avec l'app

== Todo En cours

== Todo list ==

- Service/outils? : pouvoir placer une alerte (type 'time' ou 'date' ? pour alerte dans le futur)

- pouvoir mettre une note gh (github) du type voulu
- pour le type 'select', il faudrait un select customisé (le même que dans Eventer) avec possibilité de filtrer les valeurs.
  
== Idées d'autres services ==

- git init (tout le flux, juste en donnant le nom du repo sur Github)
- Lancer un serveur de dev (`npm run dev`, `rails server`...) — terminal dédié, pas juste "jouer un script"
- Ouvrir le repo Git (statut, ou `git pull`/`push`)
- Ouvrir l'URL locale du projet dans le navigateur
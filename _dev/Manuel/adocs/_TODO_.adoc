== Réflexions ==

== Idées d'autres services ==

== Autres versions ==

- surveiller des mots ? (rêve que j'ai fait : :error quand on est en train d'écrire (ou pas : la page pourrait surveille ":" ou ":[a-z]" et passer en mode pour insérer quelque chose)). :note, :idee, :error, 
  Mais comment les taper ? une console qui permetttrait par exemple de faire ':error Ceci est une grave erreur' qui serait aussi transformé en service gh-create-issue de type 'bug' ?
  Commandes possible :
    :open     Ouvrir le projet
    :versionne minor  Versionner le fichier désigné
    :<custom> associé à service 
    :run Actualiser le code       Joue le service appelé "Actualiser le code" dans le projet
    :run item claude            Fonctionne en toute casse
    Mais est-ce que c'est vraiment intéressant alors que prendre la souris serait plus rapide ?
